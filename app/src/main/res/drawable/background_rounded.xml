<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Définir la couleur de fond -->
    <solid android:color="@color/accentTen" />

    <!-- Définir la bordure (facultatif) -->
    <stroke android:width="2dp" android:color="@color/accentTen" />

    <!-- Définir le rayon des coins -->
    <corners android:radius= "18dp" />

    <!-- Ajouter des marges internes (padding) (facultatif) -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>